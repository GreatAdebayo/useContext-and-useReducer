// import React, {useContext} from 'react'
// import AppContext from '../context/app-context'


// const Home = () => {
//     const {message, isAuth, setAuth} = useContext(AppContext)
//     return (
//         <div style={{backgroundColor: isAuth ? 'red' : 'green'}}>
//             <p>{message}</p>
//             {isAuth ? <button onClick={()=>setAuth(false)}>Login</button> : <button  onClick={()=>setAuth(true)}>Logout</button>}
//         </div>
//     )
// }

// export default Home
